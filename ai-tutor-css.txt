/* =========================================================
   NADAF CREATES AI TUTOR
   AI TUTOR SPECIFIC STYLES
   Works with learning.css
   ========================================================= */


/* =========================================================
   INTRODUCTION CARD
   ========================================================= */

.ai-tutor-intro {
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 30px;

    margin-bottom: 25px;
    padding: 30px 35px;

    border-radius: 22px;

    background:
        linear-gradient(
            135deg,
            rgba(255, 255, 255, 0.94),
            rgba(230, 230, 255, 0.95)
        );

    border: 1px solid rgba(138, 138, 255, 0.22);

    box-shadow:
        0 12px 30px rgba(80, 70, 150, 0.08);

    position: relative;
    overflow: hidden;
}

.ai-tutor-intro::before {
    content: "";

    position: absolute;
    top: -80px;
    right: 120px;

    width: 220px;
    height: 220px;

    border-radius: 50%;

    background: rgba(138, 138, 255, 0.09);
    pointer-events: none;
}

.ai-intro-content {
    position: relative;
    z-index: 2;
}

.ai-intro-content small {
    display: block;

    margin-bottom: 7px;

    color: #7777e8;

    font-size: 11px;
    font-weight: 700;
    letter-spacing: 1.8px;
    text-transform: uppercase;
}

.ai-intro-content h2 {
    margin: 0 0 8px;

    color: #333b42;

    font-size: 28px;
    line-height: 1.25;
    font-weight: 700;
}

.ai-intro-content p {
    max-width: 650px;

    margin: 0;

    color: #707783;

    font-size: 13px;
    line-height: 1.8;
    font-weight: 400;
}

.ai-intro-icon {
    width: 82px;
    height: 82px;

    flex-shrink: 0;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 24px;

    color: #ffffff;
    font-size: 34px;

    background:
        linear-gradient(
            145deg,
            #8a8aff,
            #6868dc
        );

    box-shadow:
        0 12px 25px rgba(138, 138, 255, 0.3);

    transform: rotate(4deg);

    position: relative;
    z-index: 2;
}

.ai-intro-icon i {
    transform: rotate(-4deg);
}


/* =========================================================
   MODE SWITCHER
   ========================================================= */

.learning-mode-switcher {
    width: 100%;

    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 8px;

    margin-bottom: 22px;
}

.mode-switch-group {
    display: inline-flex;
    align-items: center;
    gap: 8px;

    padding: 6px;

    border-radius: 15px;

    background: rgba(255, 255, 255, 0.75);
    border: 1px solid rgba(138, 138, 255, 0.16);

    box-shadow:
        0 5px 18px rgba(70, 65, 130, 0.05);
}

.mode-switch {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 9px;

    padding: 11px 17px;

    border: none;
    border-radius: 11px;

    color: #777783;
    background: transparent;

    font-family: "Poppins", sans-serif;
    font-size: 12px;
    font-weight: 600;

    cursor: pointer;

    transition:
        color 0.25s ease,
        background 0.25s ease,
        box-shadow 0.25s ease,
        transform 0.25s ease;
}

.mode-switch i {
    font-size: 13px;
}

.mode-switch:hover {
    color: #6868dc;
    background: rgba(138, 138, 255, 0.08);
}

.mode-switch.active {
    color: #ffffff;

    background:
        linear-gradient(
            135deg,
            #8a8aff,
            #7070df
        );

    box-shadow:
        0 6px 15px rgba(138, 138, 255, 0.25);
}

.mode-switch.active:hover {
    color: #ffffff;
    transform: translateY(-1px);
}

.ai-page-label {
    color: #898995;

    font-size: 11px;
    font-weight: 500;
}


/* =========================================================
   AI TUTOR WINDOW
   ========================================================= */

.ai-tutor-window {
    width: calc(100% - 100px);
    margin: 0 50px;

    height: min(500px, calc(100vh - 330px));

    min-height: 0;
    max-height: calc(100vh - 330px);

    display: flex;
    flex-direction: column;

    overflow: hidden;
    position: relative;

    border-radius: 23px;

    background: linear-gradient(
        145deg,
        #141423 0%,
        #1b1b31 45%,
        #272044 100%
    );

    border: 1px solid rgba(138, 138, 255, 0.32);
    box-sizing: border-box;
}


/* =========================================================
   CHAT HEADER
   ========================================================= */

.ai-chat-header {
    width: 100%;

    flex: 0 0 auto;

    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;

    padding: 20px 25px;

    position: relative;
    z-index: 20;

    overflow: visible;

    border-bottom: 1px solid rgba(255, 255, 255, 0.08);

    background:
        rgba(20, 20, 35, 0.55);

    backdrop-filter: blur(14px);
}


.ai-chat-title {
    display: flex;
    align-items: center;
    gap: 13px;

    min-width: 0;
}

.ai-chat-logo {
    width: 44px;
    height: 44px;

    flex-shrink: 0;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 14px;

    color: #ffffff;
    font-size: 19px;

    background:
        linear-gradient(
            135deg,
            #8a8aff,
            #5d5dc9
        );

    box-shadow:
        0 7px 18px rgba(138, 138, 255, 0.28);
}

.ai-chat-title strong {
    display: block;

    margin-bottom: 3px;

    color: #ffffff;

    font-size: 14px;
    font-weight: 700;
}

.ai-online-status {
    display: flex;
    align-items: center;
    gap: 6px;

    color: rgba(255, 255, 255, 0.48);

    font-size: 10px;
    font-weight: 400;
}

.online-dot {
    width: 6px;
    height: 6px;

    border-radius: 50%;

    background: #65d69b;

    box-shadow:
        0 0 0 4px rgba(101, 214, 155, 0.09);
}


/* =========================================================
   MODEL SELECTOR
   ========================================================= */

.ai-model-selector {
    position: relative;
    z-index: 100;
}

.model-selector-button {
    min-width: 155px;

    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 15px;

    padding: 10px 13px;

    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 11px;

    color: rgba(255, 255, 255, 0.8);
    background: rgba(255, 255, 255, 0.055);

    font-family: "Poppins", sans-serif;
    font-size: 11px;
    font-weight: 600;

    cursor: pointer;

    transition:
        background 0.2s ease,
        border-color 0.2s ease;
}

.model-selector-button:hover {
    background: rgba(138, 138, 255, 0.13);
    border-color: rgba(138, 138, 255, 0.45);
}

.model-selector-button > i {
    color: rgba(255, 255, 255, 0.45);
    font-size: 10px;

    transition: transform 0.2s ease;
}

.model-selector-left {
    display: flex;
    align-items: center;
    gap: 8px;
}

.selected-model-icon {
    width: 23px;
    height: 23px;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 7px;

    color: #ffffff;
    background: #7777e8;

    font-size: 10px;
    font-weight: 700;
}

.model-dropdown {
    position: absolute;

    top: calc(100% + 10px);
    right: 0;

    z-index: 9999;

    min-width: 205px;

    padding: 7px;

    border: 1px solid rgba(138, 138, 255, 0.25);
    border-radius: 14px;

    background:
        #24243b;

    box-shadow:
        0 18px 35px rgba(0, 0, 0, 0.35);

    opacity: 0;
    visibility: hidden;
    pointer-events: none;

    transform: translateY(-8px);

    transition:
        opacity 0.2s ease,
        visibility 0.2s ease,
        transform 0.2s ease;
}

.model-dropdown.show {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;

    transform: translateY(0);
}

.model-option {
    width: 100%;

    display: flex;
    align-items: center;
    gap: 9px;

    padding: 10px;

    border: none;
    border-radius: 9px;

    color: rgba(255, 255, 255, 0.72);
    background: transparent;

    font-family: "Poppins", sans-serif;
    font-size: 11px;
    font-weight: 500;

    text-align: left;
    cursor: pointer;

    transition:
        color 0.2s ease,
        background 0.2s ease;
}

.model-option:hover {
    color: #ffffff;
    background: rgba(138, 138, 255, 0.14);
}

.model-option.selected {
    color: #ffffff;
    background: rgba(138, 138, 255, 0.18);
}

.model-option-icon {
    width: 24px;
    height: 24px;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 7px;

    color: #ffffff;
    background: rgba(138, 138, 255, 0.65);

    font-size: 10px;
    font-weight: 700;
}

.model-check {
    margin-left: auto;

    color: #8a8aff;
    font-size: 11px;

    opacity: 0;
}

.model-option.selected .model-check {
    opacity: 1;
}


/* =========================================================
   CHAT BODY - SCROLLABLE AREA
   ========================================================= */

.ai-chat-body {
    flex: 1 1 auto;

    min-height: 0;

    position: relative;

    overflow-y: auto;
    overflow-x: hidden;

    padding: 30px;

    scroll-behavior: smooth;
    overscroll-behavior: contain;

    scrollbar-width: thin;
    scrollbar-color:
        rgba(138, 138, 255, 0.55)
        rgba(255, 255, 255, 0.04);
}

.ai-chat-body::-webkit-scrollbar {
    width: 8px;
}

.ai-chat-body::-webkit-scrollbar-track {
    border-radius: 10px;
    background: rgba(255, 255, 255, 0.04);
}

.ai-chat-body::-webkit-scrollbar-thumb {
    border-radius: 10px;
    background: rgba(138, 138, 255, 0.45);
}

.ai-chat-body::-webkit-scrollbar-thumb:hover {
    background: rgba(138, 138, 255, 0.75);
}


/* =========================================================
   WELCOME STATE
   ========================================================= */

.ai-welcome-state {
    min-height: 100%;

    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;

    padding: 10px 5px;

    text-align: center;
}

.ai-welcome-icon {
    width: 70px;
    height: 70px;

    display: flex;
    align-items: center;
    justify-content: center;

    margin-bottom: 20px;

    border-radius: 22px;

    color: #b4b4ff;
    background:
        rgba(138, 138, 255, 0.13);

    border: 1px solid rgba(138, 138, 255, 0.18);

    font-size: 28px;

    box-shadow:
        0 0 35px rgba(138, 138, 255, 0.08);
}

.ai-welcome-state h2 {
    margin: 0 0 10px;

    color: #ffffff;

    font-size: 22px;
    font-weight: 600;
}

.ai-welcome-state p {
    max-width: 500px;

    margin: 0 0 25px;

    color: rgba(255, 255, 255, 0.48);

    font-size: 12px;
    line-height: 1.8;
}


/* =========================================================
   SUGGESTED PROMPTS
   ========================================================= */

.suggested-prompts {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 9px;

    max-width: 650px;
}

.suggested-prompt {
    padding: 10px 14px;

    border: 1px solid rgba(138, 138, 255, 0.2);
    border-radius: 10px;

    color: rgba(255, 255, 255, 0.62);
    background: rgba(255, 255, 255, 0.045);

    font-family: "Poppins", sans-serif;
    font-size: 11px;
    font-weight: 500;

    cursor: pointer;

    transition:
        color 0.2s ease,
        background 0.2s ease,
        border-color 0.2s ease,
        transform 0.2s ease;
}

.suggested-prompt:hover {
    color: #ffffff;

    background: rgba(138, 138, 255, 0.14);
    border-color: rgba(138, 138, 255, 0.5);

    transform: translateY(-2px);
}


/* =========================================================
   CHAT MESSAGES
   ========================================================= */

.chat-messages {
    width: 100%;

    display: flex;
    flex-direction: column;
    gap: 22px;
}

.chat-message {
    width: 100%;

    display: flex;
    align-items: flex-start;
    gap: 12px;

    animation: messageAppear 0.3s ease forwards;
}

.chat-message.user {
    flex-direction: row-reverse;
}

.message-avatar {
    width: 32px;
    height: 32px;

    flex-shrink: 0;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 10px;

    color: #ffffff;
    background: rgba(138, 138, 255, 0.7);

    font-size: 12px;
    font-weight: 700;
}

.chat-message.user .message-avatar {
    background: rgba(255, 255, 255, 0.13);
    color: rgba(255, 255, 255, 0.75);
}

.message-content {
    max-width: min(75%, 650px);

    padding: 13px 16px;

    border-radius: 15px;

    color: rgba(255, 255, 255, 0.78);

    font-size: 12px;
    line-height: 1.8;
    font-weight: 400;

    background: rgba(255, 255, 255, 0.06);

    border: 1px solid rgba(255, 255, 255, 0.06);
}

.chat-message.user .message-content {
    color: #ffffff;

    background:
        linear-gradient(
            135deg,
            rgba(138, 138, 255, 0.8),
            rgba(104, 104, 220, 0.8)
        );

    border-color: rgba(138, 138, 255, 0.4);

    border-top-right-radius: 5px;
}

.chat-message.ai .message-content {
    border-top-left-radius: 5px;
}

@keyframes messageAppear {
    from {
        opacity: 0;
        transform: translateY(8px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}


/* =========================================================
   TYPING INDICATOR
   ========================================================= */

.ai-typing-indicator {
    display: none;

    align-items: center;
    gap: 12px;

    margin-top: 22px;
}

.ai-typing-indicator.show {
    display: flex;
}

.typing-avatar {
    width: 32px;
    height: 32px;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 10px;

    color: #ffffff;
    background: #7777e8;

    font-size: 12px;
    font-weight: 700;
}

.typing-bubble {
    display: flex;
    align-items: center;
    gap: 5px;

    padding: 13px 16px;

    border-radius: 14px;

    background: rgba(255, 255, 255, 0.07);
}

.typing-bubble span {
    width: 6px;
    height: 6px;

    border-radius: 50%;

    background: #9b9bff;

    animation: typingBounce 1.2s infinite ease-in-out;
}

.typing-bubble span:nth-child(2) {
    animation-delay: 0.15s;
}

.typing-bubble span:nth-child(3) {
    animation-delay: 0.3s;
}

@keyframes typingBounce {
    0%,
    60%,
    100% {
        opacity: 0.35;
        transform: translateY(0);
    }

    30% {
        opacity: 1;
        transform: translateY(-4px);
    }
}


/* =========================================================
   VOICE MODE PANEL
   ========================================================= */

.voice-mode-panel {
    min-height: 100%;

    display: none;

    flex-direction: column;
    align-items: center;
    justify-content: center;

    padding: 35px 20px;

    text-align: center;
}

.voice-mode-panel.active {
    display: flex;
}

.voice-visualizer {
    width: 150px;
    height: 150px;

    display: flex;
    align-items: center;
    justify-content: center;

    position: relative;

    margin-bottom: 25px;
}

.voice-ring {
    position: absolute;

    width: 100%;
    height: 100%;

    border: 1px solid rgba(138, 138, 255, 0.35);
    border-radius: 50%;

    animation: voiceRingPulse 2.6s infinite ease-out;
}

.voice-ring.ring-two {
    animation-delay: 0.85s;
}

.voice-ring.ring-three {
    animation-delay: 1.7s;
}

.voice-center-icon {
    width: 72px;
    height: 72px;

    display: flex;
    align-items: center;
    justify-content: center;

    border-radius: 50%;

    color: #ffffff;
    background:
        linear-gradient(
            135deg,
            #8a8aff,
            #6262d4
        );

    font-size: 27px;

    box-shadow:
        0 0 35px rgba(138, 138, 255, 0.35);

    position: relative;
    z-index: 2;
}

@keyframes voiceRingPulse {
    0% {
        opacity: 0.65;
        transform: scale(0.65);
    }

    100% {
        opacity: 0;
        transform: scale(1.35);
    }
}

.voice-mode-panel h2 {
    margin: 0 0 10px;

    color: #ffffff;

    font-size: 22px;
    font-weight: 600;
}

.voice-mode-panel p {
    max-width: 430px;

    margin: 0 0 25px;

    color: rgba(255, 255, 255, 0.5);

    font-size: 12px;
    line-height: 1.8;
}

.start-voice-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 9px;

    padding: 12px 20px;

    border: none;
    border-radius: 11px;

    color: #ffffff;

    background:
        linear-gradient(
            135deg,
            #8a8aff,
            #6666d5
        );

    font-family: "Poppins", sans-serif;
    font-size: 12px;
    font-weight: 600;

    cursor: pointer;

    box-shadow:
        0 8px 20px rgba(138, 138, 255, 0.22);

    transition:
        transform 0.2s ease,
        box-shadow 0.2s ease;
}

.start-voice-button:hover {
    transform: translateY(-2px);

    box-shadow:
        0 12px 25px rgba(138, 138, 255, 0.32);
}


/* =========================================================
   CHAT INPUT AREA
   ========================================================= */

.ai-chat-input-area {
    flex: 0 0 auto;

    width: 100%;

    padding: 15px 25px 12px;

    position: relative;
    z-index: 15;

    background:
        linear-gradient(
            to top,
            rgba(20, 20, 35, 0.98),
            rgba(20, 20, 35, 0.78)
        );

    border-top: 1px solid rgba(255, 255, 255, 0.07);
}

.chat-input-wrapper {
    width: 100%;

    display: flex;
    align-items: flex-end;
    gap: 8px;

    padding: 8px 9px 8px 13px;

    border: 1px solid rgba(255, 255, 255, 0.13);
    border-radius: 16px;

    background: rgba(255, 255, 255, 0.055);

    transition:
        border-color 0.2s ease,
        background 0.2s ease;
}

.chat-input-wrapper:focus-within {
    border-color: rgba(138, 138, 255, 0.65);
    background: rgba(255, 255, 255, 0.075);
}

#chatInput {
    flex: 1;

    min-width: 0;
    max-height: 125px;
    min-height: 25px;

    resize: none;

    padding: 5px 4px;

    border: none;
    outline: none;

    color: #ffffff;
    background: transparent;

    font-family: "Poppins", sans-serif;
    font-size: 12px;
    line-height: 1.7;
}

#chatInput::placeholder {
    color: rgba(255, 255, 255, 0.35);
}

#chatInput::-webkit-scrollbar {
    width: 5px;
}

#chatInput::-webkit-scrollbar-thumb {
    background: rgba(138, 138, 255, 0.5);
    border-radius: 10px;
}

.input-action-button,
.send-message-button {
    width: 35px;
    height: 35px;

    flex-shrink: 0;

    display: flex;
    align-items: center;
    justify-content: center;

    border: none;
    border-radius: 10px;

    font-size: 13px;

    cursor: pointer;

    transition:
        color 0.2s ease,
        background 0.2s ease,
        transform 0.2s ease;
}

.input-action-button {
    color: rgba(255, 255, 255, 0.42);
    background: transparent;
}

.input-action-button:hover {
    color: #ffffff;
    background: rgba(138, 138, 255, 0.14);
}

.input-action-button.microphone-active {
    color: #ffffff;
    background: rgba(220, 100, 130, 0.7);

    animation: microphoneBlink 1.2s infinite;
}

@keyframes microphoneBlink {
    0%,
    100% {
        box-shadow: 0 0 0 rgba(220, 100, 130, 0);
    }

    50% {
        box-shadow: 0 0 0 5px rgba(220, 100, 130, 0.12);
    }
}

.send-message-button {
    color: #ffffff;

    background:
        linear-gradient(
            135deg,
            #8a8aff,
            #6666d5
        );

    box-shadow:
        0 5px 13px rgba(138, 138, 255, 0.2);
}

.send-message-button:hover {
    transform: translateY(-2px);

    box-shadow:
        0 8px 18px rgba(138, 138, 255, 0.3);
}

.send-message-button:active {
    transform: translateY(0);
}

.ai-input-hint {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 15px;

    padding: 8px 4px 0;

    color: rgba(255, 255, 255, 0.27);

    font-size: 9px;
    font-weight: 400;
}

.ai-input-hint span {
    display: flex;
    align-items: center;
    gap: 5px;
}

.ai-input-hint i {
    font-size: 9px;
}


/* =========================================================
   AI WINDOW FOOTER
   ========================================================= */

.ai-window-footer {
    flex: 0 0 auto;

    width: 100%;

    padding: 0 25px 14px;

    color: rgba(255, 255, 255, 0.24);

    font-size: 9px;
    line-height: 1.5;

    text-align: center;

    background: #141423;
}


/* =========================================================
   PAGE FOOTER
   ========================================================= */

.page-footer {
    width: 100%;

    margin-top: 25px;
    padding-bottom: 20px;

    color: #9999a8;

    font-size: 11px;
    font-weight: 400;

    text-align: center;
}


/* =========================================================
   RESPONSIVE - BELOW 1200px
   ========================================================= */

@media (max-width: 1200px) {

    .ai-tutor-window {
        width: calc(100% - 100px);
        margin-left: 50px;
        margin-right: 50px;
    }

}


/* =========================================================
   RESPONSIVE - BELOW 950px
   ========================================================= */

@media (max-width: 950px) {

    .ai-tutor-intro {
        padding: 25px;
    }

    .ai-intro-content h2 {
        font-size: 24px;
    }

    .learning-mode-switcher {
        align-items: flex-start;
        flex-direction: column;
    }

    .ai-page-label {
        padding-left: 5px;
    }

    .ai-tutor-window {
        width: calc(100% - 40px);
        margin-left: 20px;
        margin-right: 20px;
        height: 520px;
        min-height: 500px;
    }
}


/* =========================================================
   LARGE DESKTOP - ABOVE 1400px
   ========================================================= */

@media (min-width: 1401px) {
    .ai-tutor-window {
        width: calc(100% - 100px);
        margin-left: 50px;
        margin-right: 50px;

        height: 700px;
        min-height: 700px;
    }
}


/* =========================================================
   LAPTOP / SMALL DESKTOP
   ========================================================= */

@media (max-width: 1400px) {
    .ai-tutor-window {
        width: calc(100% - 80px);
        margin-left: 40px;
        margin-right: 40px;

        height: 650px;
        min-height: 650px;
    }
}


/* =========================================================
   TABLET / BELOW 1200px
   ========================================================= */

@media (max-width: 1200px) {
    .ai-tutor-window {
        width: calc(100% - 60px);
        margin-left: 30px;
        margin-right: 30px;

        height: 620px;
        min-height: 620px;
    }
}


/* =========================================================
   TABLET / BELOW 950px
   ========================================================= */

@media (max-width: 950px) {
    .ai-tutor-window {
        width: calc(100% - 40px);
        margin-left: 20px;
        margin-right: 20px;

        height: 600px;
        min-height: 600px;

        border-radius: 20px;
    }

    .ai-chat-header {
        padding: 18px 20px;
    }

    .ai-chat-body {
        padding: 25px 20px;
    }
}


/* =========================================================
   MOBILE / BELOW 700px
   ========================================================= */

@media (max-width: 700px) {
    .ai-tutor-window {
        width: 100%;
        margin-left: 0;
        margin-right: 0;

        height: 580px;
        min-height: 580px;

        border-radius: 18px;
    }

    .ai-chat-header {
        padding: 15px;
    }

    .ai-chat-body {
        padding: 20px 15px;
    }

    .ai-chat-input-area {
        padding: 12px 15px 10px;
    }
}


/* =========================================================
   SMALL MOBILE / BELOW 450px
   ========================================================= */

@media (max-width: 450px) {
    .ai-tutor-window {
        height: 540px;
        min-height: 540px;

        border-radius: 15px;
    }

    .ai-chat-header {
        gap: 8px;
        padding: 13px 12px;
    }

    .ai-chat-body {
        padding: 16px 11px;
    }

    .ai-chat-input-area {
        padding: 10px 10px 8px;
    }

    .ai-window-footer {
        padding: 0 12px 10px;
    }
}

@media (max-width: 1200px) {
    .ai-tutor-window {
        width: calc(100% - 60px);
        margin-left: 30px;
        margin-right: 30px;
        height: min(480px, calc(100vh - 330px));
        max-height: calc(100vh - 330px);
    }
}

@media (max-width: 950px) {
    .ai-tutor-window {
        width: calc(100% - 40px);
        margin-left: 20px;
        margin-right: 20px;
        height: min(460px, calc(100vh - 300px));
        max-height: calc(100vh - 300px);
    }
}

@media (max-width: 700px) {
    .ai-tutor-window {
        width: 100%;
        margin: 0;
        height: min(450px, calc(100vh - 280px));
        max-height: calc(100vh - 280px);
        border-radius: 18px;
    }
}

@media (max-width: 450px) {
    .ai-tutor-window {
        height: min(420px, calc(100vh - 260px));
        max-height: calc(100vh - 260px);
    }
}


/* =========================================================
   REDUCED MOTION
   ========================================================= */

@media (prefers-reduced-motion: reduce) {

    *,
    *::before,
    *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        scroll-behavior: auto !important;
        transition-duration: 0.01ms !important;
    }

}

.main-content {
    min-height: 100vh;
    box-sizing: border-box;
    padding-bottom: 25px;
}

.ai-chat-body {
    flex: 1 1 auto;
    min-height: 0;
    overflow-y: auto;
    overflow-x: hidden;
}
